import { WorkOpeningSkeleton } from "@/components/WorkOpeningSkeleton";
import { COPY } from "@/lib/copy";

export default function PainelLoading() {
  return <WorkOpeningSkeleton label={COPY.painelOpening} />;
}
